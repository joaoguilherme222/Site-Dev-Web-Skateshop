function darkMode() {
    // Nessas duas linhas de código eu defino uma varíavel e quando a funcão em questão é chamada ele adiciona a classe "darkMode" ao elemento body, para dar toggle no modo escuro.
    var element = document.body;
    element.classList.toggle("darkMode")
}